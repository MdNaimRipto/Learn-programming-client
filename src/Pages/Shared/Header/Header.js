import React from 'react';
import Nav from '../Nav/Nav';

const header = () => {
    return (
        <div>
            <Nav></Nav>
            <hr className='container mx-auto' />
        </div>
    );
};

export default header;